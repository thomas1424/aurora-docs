import React from 'react';
import { CodeBlock, HeroSection } from '../App'; // We will export these from App.js

const Intro = () => (
  <>
    <HeroSection />
    <div className="content-container">
      {/* You can add a brief welcome section here if you like */}
    </div>
  </>
);

export default Intro;